void	begin(t_tetris *t)
{
	short	*map;
	int		square;
//	calcul carré minimum en fonction du nombre de piece
//	si backtracking echec donc piece precedente a bouger => augmenter tailler carré minim
}

int backtracking(t_tetris *t, int square, short *map, int p)
{
	backtracking(t, p + 1);
	if (!map)
		map[13] = (short[13]){0};
}
