#include "fillit.h"

/*char    *filling_map(t_tetri *t, t_map *m)
{
    //m->x = t->nb;

    while (m->map)
    {

        m->map++;
    }

    return (m->map);
}*/

char    *resize_map(t_tetri *t, t_map *m)
{
    m->x = t->nb;

    return ("imotep");
}

int    cmp_map(t_tetri *t, t_map *m)
{
  m->x = t->nb;
  /*int i;
  int j;

  i = -1;
  while (m->map[++i] == 0)
  {
    j = -1;
    while (m->map[i][++j] == 0)
    {

    }
  }*/
  return (-1);
}
